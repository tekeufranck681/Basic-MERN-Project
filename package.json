{
  "name": "product-add-webapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development nodemon backEnd/server.js",
    "build":"npm install && npm install --prefix frontEnd && npm run build --prefix frontEnd",
    "start":"kill -9 $(lsof -t -i:5000) && NODE_ENV=production node backEnd/server.js"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
